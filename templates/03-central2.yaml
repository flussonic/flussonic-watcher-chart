{{- if .Values.central.create -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-central2
  labels:
    name: {{ .Release.Name }}-central2
spec:
  ports:
  - port: 80
    targetPort: 3020
    name: central2
  selector:
    app: {{ .Release.Name }}-central2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-central2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-central2
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-central2
    spec:
      nodeSelector:
        flussonic.com/database: "true"
      containers:
      - name: {{ .Release.Name }}-central2
        image: "central2:1"
        imagePullPolicy: Never
        env:
        - name: CENTRAL_API_KEY
          value: apikey0
        - name: CENTRAL_TOKEN
          value: "npIBeaDH6uTkR2"
        - name: CENTRAL_CLUSTER_NODE_CONFIG_PROVISION_ENABLED
          value: "false"
        - name: CENTRAL_URL
          value: "http://{{ .Release.Name }}-central.{{ $.Release.Namespace }}.svc.cluster.local"
{{- end }}
