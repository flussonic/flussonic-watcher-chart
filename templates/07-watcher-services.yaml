{{- if .Values.watcher.create -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: {{ .Release.Name }}-worker
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-worker
    spec:
      containers:
      - name: worker
        image: "{{ .Values.watcher.repository }}:{{ .Values.watcher.tag }}"
        args:
        - worker
        env:
        - name: DB
          value: postgresql://test:test@{{ .Release.Name }}-postgres.{{ $.Release.Namespace }}.svc.cluster.local:5432/test_c
        - name: REDIS
          value: redis://{{ .Release.Name }}-redis.{{ $.Release.Namespace }}.svc.cluster.local:9017
        - name: CENTRAL_URL
          value: http://apikey0@{{ .Release.Name }}-central.{{ $.Release.Namespace }}.svc.cluster.local/streamer/api/v3
        - name: PORT
          value: "5000"
        - name: LICENSE_KEY
          valueFrom:
            secretKeyRef:
              name: flussonic-license
              key: license_key
              optional: false


---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-scheduler
spec:
  serviceName: {{ .Release.Name }}-scheduler
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-scheduler
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-scheduler
    spec:
      containers:
      - name: scheduler
        image: "{{ .Values.watcher.repository }}:{{ .Values.watcher.tag }}"
        args:
        - scheduler
        env:
        - name: DB
          value: postgresql://test:test@{{ .Release.Name }}-postgres.{{ $.Release.Namespace }}.svc.cluster.local:5432/test_c
        - name: REDIS
          value: redis://{{ .Release.Name }}-redis.{{ $.Release.Namespace }}.svc.cluster.local:9017
        - name: CENTRAL_URL
          value: http://apikey0@{{ .Release.Name }}-central.{{ $.Release.Namespace }}.svc.cluster.local/streamer/api/v3
        - name: LICENSE_KEY
          valueFrom:
            secretKeyRef:
              name: flussonic-license
              key: license_key
              optional: false


---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-episodes
spec:
  serviceName: {{ .Release.Name }}-episodes
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-episodes
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-episodes
    spec:
      containers:
      - name: episodes
        image: "{{ .Values.watcher.repository }}:{{ .Values.watcher.tag }}"
        args:
        - episodes
        env:
        - name: DB
          value: postgresql://test:test@{{ .Release.Name }}-postgres.{{ $.Release.Namespace }}.svc.cluster.local:5432/test_c
        - name: REDIS
          value: redis://{{ .Release.Name }}-redis.{{ $.Release.Namespace }}.svc.cluster.local:9017
        - name: CENTRAL_URL
          value: http://apikey0@{{ .Release.Name }}-central.{{ $.Release.Namespace }}.svc.cluster.local/streamer/api/v3
        - name: LICENSE_KEY
          valueFrom:
            secretKeyRef:
              name: flussonic-license
              key: license_key
              optional: false
{{- end }}
