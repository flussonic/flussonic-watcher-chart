{{- if .Values.watcher.create -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "flussonic-watcher-chart.fullname" . }}-worker
spec:
  serviceName: {{ include "flussonic-watcher-chart.fullname" . }}-worker
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "flussonic-watcher-chart.fullname" . }}-worker
  template:
    metadata:
      labels:
        app: {{ include "flussonic-watcher-chart.fullname" . }}-worker
    spec:
      containers:
      - name: worker
        image: "{{ .Values.watcher.repository }}:{{ .Values.watcher.tag }}"
        args:
        - worker
        env:
        - name: DB
          value: postgresql://test:test@{{ include "flussonic-watcher-chart.fullname" . }}-postgres.{{ $.Release.Namespace }}.svc.cluster.local:5432/test_c
        - name: REDIS
          value: redis://{{ include "flussonic-watcher-chart.fullname" . }}-redis.{{ $.Release.Namespace }}.svc.cluster.local:9017
        - name: CENTRAL_URL
          value: http://apikey0@{{ include "flussonic-watcher-chart.fullname" . }}-central.{{ $.Release.Namespace }}.svc.cluster.local/streamer/api/v3
        - name: PORT
          value: "5000"
        - name: LICENSE_KEY
          valueFrom:
            secretKeyRef:
              name: flussonic-license
              key: license_key
              optional: false


---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "flussonic-watcher-chart.fullname" . }}-scheduler
spec:
  serviceName: {{ include "flussonic-watcher-chart.fullname" . }}-scheduler
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "flussonic-watcher-chart.fullname" . }}-scheduler
  template:
    metadata:
      labels:
        app: {{ include "flussonic-watcher-chart.fullname" . }}-scheduler
    spec:
      containers:
      - name: scheduler
        image: "{{ .Values.watcher.repository }}:{{ .Values.watcher.tag }}"
        args:
        - scheduler
        env:
        - name: DB
          value: postgresql://test:test@{{ include "flussonic-watcher-chart.fullname" . }}-postgres.{{ $.Release.Namespace }}.svc.cluster.local:5432/test_c
        - name: REDIS
          value: redis://{{ include "flussonic-watcher-chart.fullname" . }}-redis.{{ $.Release.Namespace }}.svc.cluster.local:9017
        - name: CENTRAL_URL
          value: http://apikey0@{{ include "flussonic-watcher-chart.fullname" . }}-central.{{ $.Release.Namespace }}.svc.cluster.local/streamer/api/v3
        - name: LICENSE_KEY
          valueFrom:
            secretKeyRef:
              name: flussonic-license
              key: license_key
              optional: false


---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "flussonic-watcher-chart.fullname" . }}-episodes
spec:
  serviceName: {{ include "flussonic-watcher-chart.fullname" . }}-episodes
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "flussonic-watcher-chart.fullname" . }}-episodes
  template:
    metadata:
      labels:
        app: {{ include "flussonic-watcher-chart.fullname" . }}-episodes
    spec:
      containers:
      - name: episodes
        image: "{{ .Values.watcher.repository }}:{{ .Values.watcher.tag }}"
        args:
        - episodes
        env:
        - name: DB
          value: postgresql://test:test@{{ include "flussonic-watcher-chart.fullname" . }}-postgres.{{ $.Release.Namespace }}.svc.cluster.local:5432/test_c
        - name: REDIS
          value: redis://{{ include "flussonic-watcher-chart.fullname" . }}-redis.{{ $.Release.Namespace }}.svc.cluster.local:9017
        - name: CENTRAL_URL
          value: http://apikey0@{{ include "flussonic-watcher-chart.fullname" . }}-central.{{ $.Release.Namespace }}.svc.cluster.local/streamer/api/v3
        - name: LICENSE_KEY
          valueFrom:
            secretKeyRef:
              name: flussonic-license
              key: license_key
              optional: false
{{- end }}
